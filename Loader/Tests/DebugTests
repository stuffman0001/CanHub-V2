
local function loadSettings()
	if game:GetService("RunService"):IsStudio() then
		return "STUDIO"
	end
	
	if isfile("CanHub/CanHub.json") then
		local settingsData = readfile("CanHub/CanHub.json")
		return game:GetService("HttpService"):JSONDecode(settingsData)
	else
		return nil
	end
end
local SETTINGS = loadSettings() or {
	general = {
		Print = true,
		SendNotifications = true,
		AutoInject = true,
	},
	safety = {
		Bypassers = true,
		UncTests = true,
	}
}	
if SETTINGS.safety.Bypassers == true then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/stuffman0001/CanHub-V2/refs/heads/main/Loader/Tests/Adonis%20Bypass.lua"))()
end
